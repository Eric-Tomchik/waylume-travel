NEXT_PUBLIC_CONVEX_URL=https://secret-heron-979.convex.cloud
CONVEX_SITE_URL=https://secret-heron-979.convex.site
# Set this to a long random secret in BOTH the web host and Convex deployment environments.
WAYLUME_ADMIN_TOKEN=replace-with-a-long-random-secret
# Recommended: separate secret used only to sign HttpOnly admin sessions.
WAYLUME_ADMIN_SESSION_SECRET=replace-with-a-different-long-random-secret
# Required in BOTH web host and Convex to prevent direct public writes to the Convex intake endpoint.
WAYLUME_INTAKE_SECRET=replace-with-another-long-random-secret
# Optional exact public origin, e.g. https://travel.example.com, for trip-request origin checks.
WAYLUME_SITE_ORIGIN=

# Optional OpenAI-backed Waylume AI concierge. Without this key, the concierge stays functional in interactive demo mode.
OPENAI_API_KEY=
# Optional model override. Defaults to gpt-5.6-luna for a cost-efficient conversational experience.
WAYLUME_AI_MODEL=gpt-5.6-luna

# Optional direct email delivery through Resend. If unset, notification webhook/queue behavior remains available.
RESEND_API_KEY=
WAYLUME_EMAIL_FROM=

# Optional outbound notification delivery webhook. When unset, messages remain safely queued in Waylume.
WAYLUME_NOTIFICATION_WEBHOOK_URL=
WAYLUME_NOTIFICATION_WEBHOOK_SECRET=

# Optional AI itinerary-draft provider webhook. When unset, Waylume uses its built-in deterministic draft builder.
WAYLUME_ITINERARY_DRAFT_WEBHOOK_URL=
WAYLUME_ITINERARY_DRAFT_WEBHOOK_SECRET=
