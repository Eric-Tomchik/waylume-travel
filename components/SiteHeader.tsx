/**
 * Legacy import path. The site now uses the Waylume Travel chrome in
 * components/wl/Chrome.tsx; this keeps older routes on the same header.
 */
export { SiteHeader as default } from "@/components/wl/Chrome";
